#include "main.h"
/**
* _isalpha - check if the char c is an alphabet or not
* @c - is te random varibe elected to check
* Return: 0 if it isnt and otherwise if it is
*/
int _isalpha(int c)
{
if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}
