#include MAIN_H
/**
 * _abs - compute the integer value of
 *  @n: to determine eitger its
 *
 *  Return: tge absolute vlue of c
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-n);
}
